{
  "name": "gift-checker",
  "private": true,
  "version": "1.0.0",
  "description": "Static site — no framework; build only copies files to dist/ for Vercel.",
  "scripts": {
    "build": "node scripts/build-static.cjs"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
